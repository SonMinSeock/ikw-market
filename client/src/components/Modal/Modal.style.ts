import styled from "styled-components";

export const ModalLayout = styled.div`
  img {
    opacity: 1;
    width: 100%;
  }
  svg {
    position: absolute;
    top: 0;
    right: 0;
    cursor: pointer;
  }
`;
